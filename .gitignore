# ── UniProgrammer ────────────────────────────────────────────────
# 依赖
node_modules/
dist/

# 闭源厂商二进制（不在仓库分发，本地构建时自行放置）
CH34X.DLL
**/CH34X.DLL

# Rust / Tauri 构建产物
3.Software/src-tauri/target/

# 环境文件
.env
.env.local
.env.*.local

# 日志与临时文件
*.log
*.tmp
*.bak

# 本地设置文件（便携版运行后生成）
Setting.set
**/Setting.set

# 插件启用状态与构建时复制进插件包的 sidecar 二进制（本地生成）
plugin-state.toml
**/plugin-state.toml
3.Software/src-tauri/plugins/builtin/uni.hal.ch34x_dll/uni_ch34x_sidecar_dll*
3.Software/src-tauri/plugins/builtin/uni.hal.ch34x_libusb/uni_ch34x_sidecar_libusb*
3.Software/src-tauri/plugins/builtin/uni.chipdb/uni_chipdb_sidecar*

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# STM32 固件编译产物（源码保留）
2.Firmware/**/*.o
2.Firmware/**/*.elf
2.Firmware/**/*.bin
2.Firmware/**/*.hex
2.Firmware/**/*.map

# 参考项目（仅本地查阅，不进仓库）
example/

# 本地讨论/规划笔记（仅本地查阅，不进仓库）
feature/

# 安装包产物（正式发布走 GitHub Actions）
*.msi
*.dmg
*.AppImage

# Portable program test directory (local only)
ProgramTest/
